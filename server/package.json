{
  "name": "server",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "format": "prettier server.js --write --single-quote"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-jwt": "^8.2.0",
    "jwks-rsa": "^3.0.0"
  },
  "devDependencies": {
    "prettier": "^1.19.1"
  }
}
